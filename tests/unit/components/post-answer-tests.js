import { test, moduleForComponent } from 'ember-qunit';

var component;

moduleForComponent('post-answer', 'Unit - Post answer component',
                   {
  setup: function() {
    component = this.subject();
  }
});

test('Write a test here', function() {
  ok( false, 'Implement Me' );
});
